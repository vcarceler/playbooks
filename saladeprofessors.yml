---
- hosts: saladeprofessors
  user: root

  tasks:
    # Tareas por defecto
    - include: default.yml

    # Cambia el hostname
    - include: cambia-hostname.yml base_hostname=saladeprofessors

    # Operaciones generales de informática
    - include: informatica.yml

